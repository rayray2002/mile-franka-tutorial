<?xml version="1.0" encoding="UTF-8" ?>
<!-- Fallback CycloneDDS config without iceoryx shared-memory transport.
     Used automatically when iox-roudi fails to start (e.g. Docker Desktop on macOS
     where POSIX ACLs on /dev/shm are not supported). Falls back to UDP. -->
<CycloneDDS xmlns="https://cdds.io/config"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://cdds.io/config
    https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/iceoryx/etc/cyclonedds.xsd">
  <Domain id="any">
    <SharedMemory>
      <Enable>false</Enable>
    </SharedMemory>
  </Domain>
</CycloneDDS>
