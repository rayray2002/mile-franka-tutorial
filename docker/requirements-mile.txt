# MILE training layer installed on top of hucebot:franka-humble.
# Keep the base image's gymnasium==0.29.1 and rclpy intact.
# mujoco 2.3.7 is MetaWorld-v2's native version; the Franka sim loop uses mujoco_ros
# (C++), not this Python package, so the downgrade is safe. The twin viewer is adapted
# to 2.3.7 in Task A3.
# MetaWorld is installed separately in the Dockerfile (patched setup.py).
mujoco==2.3.7
gymnasium==0.29.1
stable-baselines3<=2.3.2
imitation
wandb
tensorboard
pyspacemouse
hidapi
pupil-apriltags
opencv-python-headless
moviepy
# Optional OS-wide keyboard teleop (KeyboardDevice reader_backend="global"; default is
# the focus-window "pygame" backend). The X11 backend pulls in python-xlib; needs a live
# DISPLAY at runtime, not at import.
pynput
# ViveDevice's default reader (France-lab HTC Vive teleop); needs a running SteamVR at
# import-call time, not at pip-install time.
openvr
